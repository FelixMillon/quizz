import { NameResponseDirective } from './name-response.directive';

describe('NameResponseDirective', () => {
  it('should create an instance', () => {
    const directive = new NameResponseDirective();
    expect(directive).toBeTruthy();
  });
});
